@using Entities
@using Business
@page "/warehouses/delete/{WarehouseId}"

<DeleteWarehouseComponent WarehouseId="@WarehouseId"/>

@code {
    [Parameter]
    public string WarehouseId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        WarehouseId = WarehouseId;
    }
}