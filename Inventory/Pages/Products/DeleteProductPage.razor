@using Entities
@using Business
@page "/product/delete/{IdProduct}"

<DeleteProductComponent IdProduct="@IdProduct"/>

@code {
    [Parameter]
    public string IdProduct { get; set; }

    protected override async Task OnInitializedAsync()
    {
        IdProduct = IdProduct;
    }
}